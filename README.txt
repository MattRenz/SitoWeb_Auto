
Consegna: 

    Creare un sito web di almeno tre pagine:

        una di presentazione (personale o del sito stesso),
        una contenente una lista di elementi, dipendenti dall'argomento prescelto, con varidati correlati (organizzata come una lista di card libere o tabularmente)
        un template da utilizzare come pagina di dettaglio per ciascuno degli elementidella lista.
    
    Gli stili potranno essere definiti a preferenza tramite framework css (
    Bootstrap o Tailwind) o con css libero

   Java Script:

    Creare una funzione che gestisca il click su un bottone e mostriun messaggio di saluto personalizzato: 

        Esercizio svolto dentro la pagina principale, al click del bottone "Acquista"

    Creare una pagina web dinamica che mostri i dati filtrati in basealla scelta dell'utente

        Esercizio svolto nella pagina web nella sezione Menu --> Catalogo --> Ferrari
        E' stata creata una tabella e l'utente può scegleire come ordinare la tabella in base ai dati inseriti all'interno


Possibile problema:


    L'esercizio principale è stato svolto tuttavia il sito è ancora in fase di sviluppo, 
    pertanto alcune pagine web, sezioni e mediaquery non sono ottimizzate al 100% ma ancora in fase di completamento

    ---------------------------------------------------------------------------

    Per il collegamento di foto,video, e pagine web secondarie sono tutte salvate all'interno della cartella principale.

    pertanto aprire la cartella principale contenente tutte le cartelle e poi lanciare index.html per vedere il sito con i collegamenti
    giusti